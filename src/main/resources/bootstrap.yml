#SPRING
spring:
  application:
    name: microservicio-feign
  cloud:
    config:
      fail-fast: true
      enabled: true
      uri: http://user:password@config-server-lab-dekra-lab-dekra.e4ff.pro-eu-west-1.openshiftapps.com
